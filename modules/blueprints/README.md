# ⚠️ DEPRECATED

This module is deprecated. Use [`modules/blueprint`](../blueprint/) (singular) instead.

The singular module configures one blueprint per invocation, supports multi-region parameters, and includes policy grants. Invoke it multiple times for different blueprints.

This module will be removed in a future release.
